import { Color.DirectiveDirective } from './color.directive.directive';

describe('Color.DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new Color.DirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
